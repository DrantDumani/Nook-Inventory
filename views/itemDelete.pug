extends layout 

block content
  div
    h1 #{singleItem.name}
    p #{singleItem.inStock} remaining.

    p Are you sure you want to delete this item?
      strong (This action is irreversible!)
    form(method="POST")
      input(type="hidden" value=singleItem._id name="itemId")
    
      button Delete